import { AppearanceClassPipe } from './appearance-class.pipe';

describe('AppearanceClassPipe', () => {
  it('create an instance', () => {
    const pipe = new AppearanceClassPipe();
    expect(pipe).toBeTruthy();
  });
});
