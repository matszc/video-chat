import { ColorClassPipe } from './color-class.pipe';

describe('ColorClassPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorClassPipe();
    expect(pipe).toBeTruthy();
  });
});
