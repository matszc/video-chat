import { ButtonTypePipe } from './button-type.pipe';

describe('ButtonTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ButtonTypePipe();
    expect(pipe).toBeTruthy();
  });
});
