import { FieldDirective } from './field.directive';

describe('ZulopolyFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new FieldDirective();
    expect(directive).toBeTruthy();
  });
});
